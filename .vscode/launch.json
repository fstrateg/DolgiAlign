{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "1C: Pack and Run",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}\\run.bat",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Pack EPF"
    }
  ]
}
